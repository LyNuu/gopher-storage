JWT_SECRET=replace-with-long-random-string

ALLOWED_ORIGINS=https://lynuu.github.io
# Пароли БД .
AUTH_DB_PASSWORD=replace-me
STORAGE_DB_PASSWORD=replace-me
CELLS_DB_PASSWORD=replace-me

# Публичный URL Pydio Cells (с портом 8070). Один на двоих:
# идёт и в CELLS_EXTERNAL (Cells), и в PYDIO_BASE_URL (storage) — поэтому всегда совпадают.
# Если разойдутся — S3-загрузка падает с 403 SignatureDoesNotMatch.
CELLS_PUBLIC_URL=https://138.16.175.157.sslip.io:8070

# Personal Access Token администратора Pydio Cells.
PYDIO_API_KEY=get-from-cells-after-setup
